{
  "name": "angular-schematics-extension",
  "displayName": "Angular Schematics",
  "description": "在Angular项目中快速生成原理图",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:angular-schematics.generate",
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "angular-schematics.generate",
        "title": "Angular 原理图",
        "category": "Angular"
      },
      {
        "command": "case.showModal",
        "title": "Case: 打开对话框",
        "category": "Case"
      },
      {
        "command": "case.showWizard",
        "title": "Case: 向导(原生)",
        "category": "Case"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "angular-schematics.generate",
          "group": "navigation",
          "when": "workspaceFolderCount > 0"
        }
      ],
      "commandPalette": [
        {
          "command": "angular-schematics.generate",
          "when": "workspaceFolderCount > 0"
        }
      ],
      "editor/context": [
        {
          "command": "angular-schematics.generate",
          "group": "navigation",
          "when": "workspaceFolderCount > 0"
        }
      ]
    },
    "views": {
      "explorer": [
        {
          "id": "testCase",
          "name": "Case",
          "type": "webview",
          "when": "true"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "compile": "tsc -p ./",
    "build": "node esbuild.config.js",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "16.x",
    "@types/vscode": "^1.74.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@vscode/test-electron": "^2.3.8",
    "esbuild": "^0.19.0",
    "eslint": "^8.0.0",
    "glob": "^10.0.0",
    "mocha": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@angular-devkit/schematics": "^17.0.0",
    "@angular/cli": "^17.0.0",
    "@vscode/ripgrep": "^1.17.0"
  }
}
